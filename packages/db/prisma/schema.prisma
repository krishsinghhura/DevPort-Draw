generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://neondb_owner:npg_GZLFzR6f8gSQ@ep-bold-glitter-add23qfw-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
}

model User {
  id         String        @id @default(uuid())
  name       String?
  email      String        @unique
  password   String
  createdAt  DateTime      @default(now())
  rooms      Room[]        @relation("UserRooms")
  chats      ChatHistory[]
  adminRooms Room[]        @relation("AdminRooms")
}

model Room {
  id        String        @id @default(uuid())
  slug      String        @unique
  createdAt DateTime      @default(now())
  adminId   String
  admin     User          @relation("AdminRooms", fields: [adminId], references: [id])
  chats     ChatHistory[]

  members         User[]    @relation("UserRooms")
  isPublic        Boolean   @default(false)
  publicExpiresAt DateTime?
}

model ChatHistory {
  id        String   @id @default(uuid())
  message   String
  userId    String
  roomId    String
  createdAt DateTime @default(now())
  room      Room     @relation(fields: [roomId], references: [id])
  user      User     @relation(fields: [userId], references: [id])
}
